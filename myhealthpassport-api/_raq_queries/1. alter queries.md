#  Remove Uique


ALTER TABLE questionnaire
DROP CONSTRAINT questionnaire_question_key;


# dental screening

-- First, drop the problematic constraints
ALTER TABLE public.dental_screening DROP CONSTRAINT "dental_screening_doctor_id_fkey";
ALTER TABLE public.dental_screening DROP CONSTRAINT "fk_dental_s_users_sc_f046a3f7";

# Eye Screening

-- Step 1: Drop the problematic constraints
ALTER TABLE public.eye_screening DROP CONSTRAINT "eye_screening_doctor_id_fkey";
ALTER TABLE public.eye_screening DROP CONSTRAINT "fk_eye_scre_users_sc_62f62c95";



# To Change Patient Parent to Student in Consultations

-- Step 1: Drop the existing foreign key constraint.
ALTER TABLE consultations
DROP CONSTRAINT consultations_patient_id_fkey;

-- Step 2: Add the new foreign key constraint to the students table.
-- Assuming the primary key column in the 'students' table is 'id'.
-- Replace 'id' with the actual primary key column name if it is different.
ALTER TABLE consultations
ADD CONSTRAINT consultations_patient_id_fkey
FOREIGN KEY (patient_id)
REFERENCES students(id)
ON DELETE CASCADE;